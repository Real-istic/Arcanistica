let uiElements = new UiElements(
    // statusbars
    [
        // gets pushed in the buildUI function - (game.js)
    ],
    // icons
    [
        // gets pushed in the buildUI function - (game.js)
    ],
    // frames
    [
        // gets pushed in the buildUI function - (game.js)
    ]
)