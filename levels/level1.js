const level1 = new Level(

    /**
     * enemies
     */
    [
        // gets pushed in the pushEnemiesToLevel function - (game.js)
    ],

    /**
     * clouds (path, X-Position)
     */
    [
        // gets pushed in the pushCloudsToLevel function - (game.js)
    ],

    /**
     * backgrounds (path, X-Position)
     */
    [
        // gets pushed in the pushBackgroundsToLevel function - (game.js)
    ]
)